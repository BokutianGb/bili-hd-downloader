# B站最高画质下载器 —— 从设计到上线的完整经验记录

> 写于 2026-08-13。记录这个工具从"想清楚要什么"到"GitHub 上能下载"的每一步。
> 目的：下次做类似工具分发时，直接翻这份文档，少走弯路。

---

## 一、这个项目到底要解决什么

用户用 B 站看视频时遇到的痛点：
1. 想下 1080P+ → 要登录，浏览器 F12 拷的链接几分钟就过期
2. 想下 4K/8K/杜比 → 要大会员，视频流和音频流是分开的
3. 下载后剪辑/分析 → 不知道真实帧率，容器声明可能不准
4. 换台电脑 → 从头再来

**核心产品决策**：做一个 exe 双击即用的工具，自动抓 Cookie → 自动选最高流 → 自动合并 → 自动验参。

---

## 二、架构设计时的关键决策（做之前想清楚）

### 决策 1：yt-dlp 不冻结进 exe

**为什么**：B 站接口经常变，yt-dlp 必须保持可更新。如果冻结进 exe，用户每次更新都得重下整个 exe。
**方案**：exe 首次运行自动下载 yt-dlp.exe + ffmpeg.exe 到本地 `tools/` 目录。以后删掉 tools/ 重开即自动更新。
**教训**：这个决策事后证明是对的。B 站接口确实频繁变化，冻结 yt-dlp 是死路。

### 决策 2：cookie 只在用户本机生成

**为什么**：cookie 含登录凭证（SESSDATA），如果上传 GitHub 等于把 B 站账号送出去。
**方案**：
- exe 运行时从 Edge 浏览器抓 cookie 到 `cookies.txt`
- .gitignore 排除 cookies.txt
- 发布包脚本内置防御性检查（发现 cookies.txt 直接拒绝打包）

### 决策 3：下载后自动验参

**为什么**：延续用户之前"4503帧 vs 4509帧"的硬核验证习惯。不是信网页上标的数字，是 ffprobe -count_frames 逐帧数出来的。
**方案**：下载完成自动调用 ffprobe 输出真实帧率、帧数、编码等参数。

### 决策 4：GUI 用 Tkinter 不用 PyQt

**为什么**：Tkinter 是 Python 标准库，零额外依赖，打包进单文件 exe 不膨胀。PyQt 打包后体积至少 50MB+。
**教训**：这个选择是对的，最终 exe 只有 9.2MB。

---

## 三、开发过程中遇到的所有 Bug 和修复

### Bug 1：bili_cookie.py 语法错误（开发期）

**现象**：Write 工具写文件时 `return True, "..." % (...)` 跨行，报语法错误。
**原因**：Python 字符串跨行时没有正确处理续行。
**修复**：把 return 拆成两行，先构造 info 字符串再 return。

**教训**：用 AI 写代码时，长字符串拼接特别容易出这种低级语法错误。写完先编译检查再运行。

### Bug 2：SearchReplace 误删 except 块（工具使用事故）

**现象**：修复语法错误时，把 `except Exception` 也替换掉了，报 "Try 语句必须至少有一个 except 或 finally 子句"。
**原因**：SearchReplace 的 original_text 匹配范围太大，把不该替换的也替换了。
**修复**：重新读文件，手动补回 except 块。

**教训**：用 SearchReplace 时，original_text 要精确到最小匹配范围，宁多勿少。如果不确定，先读文件确认上下文。

### Bug 3：Netscape cookies.txt 格式不合法（核心 Bug，真实测试暴露）

**现象**：yt-dlp 报 `invalid Netscape format cookies file`，标准库 `http.cookiejar` 也崩了。
**根因**：Python 3.11 标准库 `http.cookiejar._really_load` 有硬断言：
```
assert domain_specified == initial_dot
```
意思是：Netscape 格式第二列写 TRUE（域 cookie）时，域名必须以 `.` 开头；host-only cookie 则必须不带点且第二列 FALSE。

旧脚本 `_cdp_get_cookies.py` 把 CDP 的 domain 统一去点后一律写 TRUE，违反了这个断言。

**修复**（在 `bili_cookie.py` 的 `write_netscape_cookies` 中）：
```python
raw = c["domain"]
if raw.startswith("."):
    domain, domain_flag = raw, "TRUE"   # 带点 = 域cookie
else:
    domain, domain_flag = raw, "FALSE"  # 不带点 = host-only
```

**验证方法**：用 `MozillaCookieJar._really_load` 单测模拟加载，确认 2 个 cookie 成功。

**教训**：
1. 不要凭"看起来对"就交付，要用标准库实际加载验证
2. 旧代码 `_cdp_get_cookies.py` 的格式一直是错的，但用户一直没发现（因为 yt-dlp 报错信息不明显）
3. 这个 bug 潜伏了很久，直到这次做真实下载测试才暴露

### Bug 4：中文文件名"乱码"假象（打包期）

**现象**：PyInstaller 打包后，os.listdir 显示文件名乱码 `'Bվ߻.exe'`。
**排查过程**：
1. 先用英文名打包（PYI_NAME="BiliHDDownloader"），再重命名为中文 → 还是"乱码"
2. 怀疑 build.py 源文件编码损坏 → 检查字节，UTF-8 正确
3. 用码点验证：`[hex(ord(c)) for c in filename[:3]]` = `0x42 0x7ad9 0x6700` = B站最，与目标完全一致
4. `equals_target: True`

**真相**：文件名完全正确，乱码只是 **PowerShell 输出管道 GBK → UTF-8 解码的显示假象**。

**修复**：保留英文名打包 + Python 内 `Path.replace` 重命名的方案（更稳定）。

**教训**：
1. **不要相信 PowerShell 终端的中文显示**——它经常把 UTF-8 按 GBK 解码导致乱码
2. 验证文件名用 `==` 比较或码点对比，不要用眼睛看终端输出
3. 这个"乱码"折腾了很长时间，实际根本不是 bug

### Bug 5：Edge WebSocket 403 Forbidden（部署后用户反馈）

**现象**：用户运行 exe 点"抓取 Cookie"时报错：
```
Handshake status 403 Forbidden
Rejected an incoming WebSocket connection from the http://127.0.0.1:9222 origin.
```

**根因**：新版 Edge 的安全策略变更——启动调试端口后，默认拒绝来自非浏览器的 WebSocket 连接。必须显式加 `--remote-allow-origins=*` 参数。

**修复**：在 `start_debug_edge()` 的 Edge 启动参数中增加一行：
```python
"--remote-allow-origins=*",
```

**教训**：
1. **浏览器 API 是会变的**——即使之前测试通过，新版本 Edge 可能改变安全策略
2. 这种 bug 只有在用户实际运行时才暴露，本地测试环境可能用的是旧版 Edge
3. 以后做浏览器自动化，要关注 Chrome/Edge 的版本更新日志，特别是 CDP 相关的安全变更

### Bug 6：winget 安装 gh CLI 失败

**现象**：`winget install GitHub.cli` 返回 exit code 1603（MSI 需要管理员权限）。
**修复**：改用官方 zip 免安装版，下载解压到 `D:\bianchengruanjian\gh\bin\gh.exe`。

**教训**：winget 装 MSI 包经常要管理员权限，zip 版更可靠。以后优先用 zip 版。

### Bug 7：gh auth login 交互卡住

**现象**：`gh auth login --web` 启动后进程卡住，需要回答交互提示。
**修复**：设环境变量 `GH_PROMPT_DISABLED=1`，让 gh 直接输出设备码而不等交互。

**教训**：gh CLI 在非 TTY 环境（如脚本中）需要 `GH_PROMPT_DISABLED=1` 跳过交互提示。

### Bug 8：git push 认证失败

**现象**：`gh repo create --push` 成功了，但后续 `git push` 认证失败。
**原因**：gh 登录时用了 `GH_PROMPT_DISABLED=1`，跳过了 git 凭据配置步骤。
**修复**：用临时命令行参数推送，不修改任何 git 配置：
```bash
git -c credential.helper="!D:/bianchengruanjian/gh/bin/gh.exe auth git-credential" push
```

**教训**：
1. `gh repo create --push` 和 `git push` 走的认证路径不同，前者用 gh 内部机制，后者需要 git credential helper
2. 用 `git -c` 临时参数可以在不修改 git config 的情况下完成认证，避免"全局配置污染"

### Bug 9：GitHub HTTPS 被 GFW 干扰

**现象**：git push 时 TCP 连接被重置，无法连接 GitHub。nslookup 有正确 IP，但 TCP 被 RST。
**排查过程**：
1. Test-NetConnection github.com -Port 443 → 失败
2. curl -sI https://github.com → 失败
3. ssh -T git@github.com → 网络通但 key 未注册
4. gh api（Go HTTP 栈）→ **网络通！**

**最终方案**：用 **gh CLI 的 Content API** 直接上传文件，绕过 git 的 libcurl：
```bash
# 上传文件（Base64 编码）
gh api repos/BokutianGb/bili-hd-downloader/contents/README.md -X PUT --input payload.json
```

**教训**：
1. GFW 干扰是**偶发的**——之前 `gh repo create --push` 能成功，说明不是完全阻断，而是不稳定
2. **git 的 HTTP 栈（libcurl）和 gh CLI 的 HTTP 栈（Go net/http）被干扰的程度不同**——后者更稳定
3. 遇到 git push 失败时，不要死磕 git，试试 gh api 直接操作
4. 这个经验在中国网络环境下特别有价值

### Bug 10：make_release.py 防御检查误拦截

**现象**：打包发布 zip 时，`check_no_sensitive()` 报 "发现敏感/运行时文件未清理: tools"。
**原因**：测试下载 yt-dlp 时留下的 tools/ 目录，不该进发布包。
**修复**：删除 tools/ 目录后重新打包。

**教训**：防御检查按设计工作了——这就是它的价值。宁可误报，不可放过。

---

## 四、工具链搭建经验

### 4.1 PyInstaller 打包

**最终方案**（build.py）：
```python
PYI_NAME = "BiliHDDownloader"  # PyInstaller 内部名用英文
# ... 打包 ...
src = HERE / "dist" / (PYI_NAME + ".exe")
src.replace(dist_exe)  # Python 内重命名为中文名
```

**关键参数**：
- `--onefile`：单文件 exe
- `--windowed`：无控制台窗口（GUI 程序）
- `--noconfirm --clean`：清理旧构建

**注意事项**：
- 中文 --name 经 PowerShell 传参会编码损坏，必须用英文名打包再重命名
- 打包后验证用 `python -c "import os; print(os.listdir('dist'))"` 或码点对比，不要信终端显示

### 4.2 gh CLI 免安装使用

**安装**：下载 `gh_*_windows_amd64.zip` 解压到任意目录，无需安装。

**登录**：
```bash
$env:GH_PROMPT_DISABLED=1  # 跳过交互提示
gh auth login --web --hostname github.com --git-protocol https --skip-ssh-key
# 会输出设备码，用户到 https://github.com/login/device 输入
```

**推送代码**：
```bash
git -c credential.helper="!D:/bianchengruanjian/gh/bin/gh.exe auth git-credential" push
```

**API 上传文件**（绕过 git 网络问题）：
```bash
gh api repos/{owner}/{repo}/contents/{path} -X PUT --input payload.json
```

### 4.3 项目结构

```
bili_hd_downloader/
├── main.py              # GUI 主程序（Tkinter）
├── bili_cookie.py       # CDP 抓 Cookie 模块
├── bili_download.py     # 下载与验参模块
├── build.py             # Python 打包脚本
├── build.bat            # 一键打包入口
├── make_release.py      # 发布包制作（含防御检查）
├── requirements.txt     # Python 依赖
├── .gitignore           # 排除敏感/运行时文件
├── README.md            # GitHub 项目说明
├── 使用说明.txt          # 傻瓜版操作说明
├── 上传GitHub.md         # GitHub 上传手册
├── banner.png           # 项目头图
└── dist/
    └── BiliHDDownloader_v1.0.zip  # 发布包
```

---

## 五、安全与隐私保护

### 三层防护

1. **.gitignore 排除**：`cookies.txt`、`edge_bilibili_cookies.txt`、`tools/`、`downloads/`、`dist/`、`build/`、`*.spec`、`__pycache__/`
2. **make_release.py 防御检查**：`FORBIDDEN = {"cookies.txt", "edge_bilibili_cookies.txt", "tools", "downloads"}`，存在即拒绝打包
3. **git 暂存区人工核对**：`git status --short` + `git ls-files | Select-String "cookie|tools|dist|downloads"` 确认无敏感文件

### 敏感信息边界

- cookies.txt **只在用户本机**由 exe 运行时生成
- 从未上传 GitHub
- 从未打包进 exe 或 zip
- README 中明确告知用户 cookies.txt 是登录凭证，不要外传

---

## 六、GitHub 上传与发布

### 首次上传流程

```bash
# 1. 登录（浏览器授权一次）
D:\bianchengruanjian\gh\bin\gh.exe auth login --web

# 2. 创建仓库并推送
cd D:\ending\bili_hd_downloader
D:\bianchengruanjian\gh\bin\gh.exe repo create bili-hd-downloader --public --source . --push

# 3. 上传发布包
D:\bianchengruanjian\gh\bin\gh.exe release create v1.0 "dist\BiliHDDownloader_v1.0.zip" --title "v1.0 首个发布"
```

### 以后更新版本

```bash
# 1. 重新打包
python build.py
python make_release.py

# 2. 提交推送
git add -A
git commit -m "更新说明"
git -c credential.helper="!D:/bianchengruanjian/gh/bin/gh.exe auth git-credential" push

# 3. 更新 Release（--clobber 替换旧包）
D:\bianchengruanjian\gh\bin\gh.exe release upload v1.0 "dist\BiliHDDownloader_v1.0.zip" --clobber
```

---

## 七、README 优化（为了更多 Star）

### 做了什么

1. **项目 Banner**：深蓝渐变风格头图，展示"4K · HDR · 1080P"
2. **徽章矩阵**：Star 数 / 版本 / 下载量 / 平台 / Python 版本
3. **痛点对比表**：用户想做的事 vs 实际遇到的墙
4. **验参亮点展示**：用真实数据证明不是糊弄人的
5. **技术栈表格**：图标 + 用途说明
6. **CTA 引导**：底部 Star 号召

### 心得

- GitHub 项目的**第一印象**就是 README，决定了路人会不会点 Star
- 痛点对比表比功能列表更能打动人——让人一眼对号入座
- 真实数据（4503帧 vs 4509帧）比空洞的"高质量"更有说服力
- 徽章让项目看起来专业、活跃

---

## 八、可复用的经验清单

### 编码规范

| 经验 | 适用场景 |
|---|---|
| Netscape cookies.txt 第二列 TRUE ⟺ 域名以 `.` 开头 | 所有手工生成 cookies.txt 的代码 |
| 写文件用 `encoding="utf-8"` | 所有 Python 文件操作 |
| 文件名验证用 `==` 或码点，不信终端显示 | Windows 中文文件名 |
| SearchReplace 的 original_text 要精确到最小匹配 | 用 AI 编辑代码时 |

### 打包分发

| 经验 | 适用场景 |
|---|---|
| PyInstaller 中文名用英文打包再重命名 | 所有中文名 exe 打包 |
| 不冻结频繁更新的工具（如 yt-dlp），首次运行自动下载 | 工具类 exe 分发 |
| 发布包脚本内置防御检查（FORBIDDEN 列表） | 含敏感信息的项目 |
| .gitignore + 防御检查 + 人工核对三重保护 | 任何含敏感信息的项目 |

### Git/GitHub

| 经验 | 适用场景 |
|---|---|
| `git -c credential.helper="!gh auth git-credential" push` | 免配置 git push |
| `gh api repos/.../contents/... -X PUT` 直接上传文件 | git push 网络不通时 |
| `GH_PROMPT_DISABLED=1` + `gh auth login --web` | 脚本中自动化 gh 登录 |
| winget 装 MSI 常失败，用 zip 免安装版 | Windows 安装 CLI 工具 |
| GFW 干扰是偶发的，gh 的 Go HTTP 栈比 git 的 libcurl 更稳定 | 中国网络环境 |

### 浏览器自动化

| 经验 | 适用场景 |
|---|---|
| 新版 Edge 需要 `--remote-allow-origins=*` 才能 WebSocket 连接 | CDP 自动化 |
| 用独立 `--user-data-dir` 启动调试实例，不影响日常浏览器 | 浏览器自动化 |
| CDP 的 `Network.getAllCookies` 失败时 fallback `Storage.getCookies` | Cookie 抓取 |
| 浏览器 API 会变，关注版本更新日志 | 长期维护的自动化工具 |

---

## 九、下次做类似项目时的 Checklist

### 开始前
- [ ] 想清楚：哪些东西不能冻结进 exe？（频繁更新的工具）
- [ ] 想清楚：哪些是敏感信息？怎么隔离？
- [ ] 想清楚：用户是谁？（技术人员还是小白？决定 README 写法）

### 开发中
- [ ] 写完每个模块先编译检查再运行
- [ ] 用 SearchReplace 时 original_text 精确到最小范围
- [ ] 真实链路测试（不是单元测试，是端到端）
- [ ] 敏感文件防御检查写进发布脚本

### 打包时
- [ ] PyInstaller 用英文名打包，Python 内重命名为中文
- [ ] 发布包脚本内置 FORBIDDEN 检查
- [ ] 验证文件名用码点对比，不信终端显示
- [ ] exe 自检模式（--selfcheck）确认能启动

### 上传时
- [ ] git 暂存区核对无敏感文件
- [ ] gh CLI 用 zip 免安装版
- [ ] `GH_PROMPT_DISABLED=1` 自动化登录
- [ ] `git -c credential.helper=...` 免配置 push
- [ ] 如果 git push 网络不通，用 `gh api` 直接上传

### 发布后
- [ ] README 有 banner + 徽章 + 痛点对比 + 真实数据 + CTA
- [ ] 发布包只含必要文件（exe + 说明）
- [ ] 写上更新流程文档（以后自己看）

---

## 十、项目信息

| 项目 | 值 |
|---|---|
| 仓库地址 | https://github.com/BokutianGb/bili-hd-downloader |
| 发布页 | https://github.com/BokutianGb/bili-hd-downloader/releases/tag/v1.0 |
| exe 下载 | https://github.com/BokutianGb/bili-hd-downloader/releases/download/v1.0/BiliHDDownloader_v1.0.zip |
| 本地路径 | `D:\ending\bili_hd_downloader\` |
| 打包环境 | volleyball conda 环境（Python 3.11.11） |
| gh CLI | `D:\bianchengruanjian\gh\bin\gh.exe`（zip 免安装版 2.96.0） |
| Git 提交数 | 7（截至 2026-08-13） |
| 发布包大小 | 9.0 MB |
| exe 大小 | 9.2 MB |

---

*这份文档的价值不在于"记录了这个项目做了什么"，而在于"下次做类似项目时，直接翻到对应的 Checklist 和经验清单，不用重新踩坑"。*
